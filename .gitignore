node_modules/

# pnpm writes this on install. The package is an installable plugin whose
# runtime dependencies come from the DSH host, so the lockfile is environment
# specific and is neither committed nor published.
pnpm-lock.yaml

# Local DSH profile links / packaging leftovers
*.tgz
*.log
.DS_Store
Thumbs.db
